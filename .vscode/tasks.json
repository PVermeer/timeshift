{
  "version": "2.0.0",
  "tasks": [
		{
      "type": "shell",
      "command": "sudo meson setup --buildtype=debug build_debug && sudo meson compile -C build_debug && cd build_debug && sudo meson install",
      "problemMatcher": ["$meson-gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "label": "DEBUG: Build and Install"
    }
  ]
}
